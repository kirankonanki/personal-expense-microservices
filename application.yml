server:
  port: 8082

spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/expense_db
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
